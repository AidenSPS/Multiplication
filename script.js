/* This is where we put our global variables */
var errors = [];
var errorDist = [0];
var questions = parseInt(document.getElementById("questions"));
var questionVal = questions.value;
var min = parseInt(document.getElementById("min"));
var minVal = min.value;
var max = parseInt(document.getElementById("max"));
var maxVal = max.value;